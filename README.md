Hotels K Merritts — Starter files

This folder contains a minimal starter site for Hotels K Merritts.

Files:
- `index.html` — Simple landing page stub.
- `style-improvements.css` — Lightweight styles for the page.
 - `hotelskmerrits.js` — Client-side JavaScript for booking modal and interactions.

Next steps:
- Replace placeholder text and contact info.
- Add real photos in a `photos/` subfolder and update `index.html`.
- Let me know if you want a booking form or connection to the main site.

Local features included:
- "Book Now" buttons on room cards open a booking modal.
- Bookings are saved to `localStorage` (mock persistence).
- Smooth scrolling for anchor links and simple contact mock handler.

To preview:
1. Open `d:/DESKTOP/InAmigos/Hotelskmerrits/index.html` in a browser.
2. Click "Book Now" on any room to test the booking modal.

If you want, I can add server-side booking (Node/PHP) or wire a real contact form.
